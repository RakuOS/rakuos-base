# RakuOS base image protected packages.
# These are the packages explicitly installed in rakuos-base/build_files/build.sh.
# They will be added to excludepkgs in dnf.conf, preventing the overlay from
# accidentally replacing or conflicting with core system components.
# DE images append their own packages in post-build-overlay.sh before
# generate-base-manifest is called, so this file is the combined list at build time.
#
# Do NOT list dependency packages here — only top-level explicitly installed ones.

# Kernel
kernel-cachyos
kernel-cachyos-devel-matched

# CachyOS performance
ananicy-cpp
cachyos-ananicy-rules
cachyos-settings
bore-sysctl
scx-scheds
scx-tools

# Gaming
gamemode
gamemode.i686

# Audio
pulseaudio-utils

# Kernel module support
dkms
mokutil

# Build tools (needed for akmods/dkms at runtime)
rust
cargo
gcc
make
perl
elfutils-libelf-devel
openssl-devel
git

# UI toolkit dev libs (needed to build rakuos tools)
gtk4-devel
libadwaita-devel
qt6-qtbase-devel
qt6-qtdeclarative-devel

# Containers
flatpak
podman
distrobox
podman-compose

# Hardware / peripherals
xpadneo
xone
xone-firmware
lm_sensors
v4l-utils
virtualbox-guest-additions

# Mesa (i686 variants — base image installs these explicitly)
mesa-dri-drivers.i686
mesa-va-drivers.i686
mesa-vulkan-drivers.i686
mesa-libEGL.i686
mesa-libGL.i686

# System libraries / tools
libxcrypt-compat
rsync
fuse
squashfuse
sqlite3
openssl
libnotify
inotify-tools
unzip
glibc-langpack-en

# Python
python3-pip
python3-setuptools
python3-dbus
python3-gobject

# App ecosystem
appstream
appstream-data
fwupd

# Node
nodejs
nodejs-npm

# Media
ffmpeg
